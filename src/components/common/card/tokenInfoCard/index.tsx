import React from 'react';
import styles from '@/components/common/card/tokenInfoCard/TokenInfoCard.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const TokenInfoCard = () => {
  return <div>TokenInfoCard</div>;
};

export default TokenInfoCard;
